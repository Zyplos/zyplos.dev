import { h } from "preact";

const Separator = ({ children, margin = "24px" }) => <div style={{ border: "1px solid #515151", marginTop: margin, marginBottom: margin }}></div>;

export default Separator;
